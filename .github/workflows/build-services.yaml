name: "Build and push services"
on:
  push:
    branches:
      - main
    tags:
      - v*
    paths-ignore:
      - "api/**"
      - "docs/**"
      - "deployments/**"
      - "features/**"
      - "scripts/**"
      - "*.md"

  pull_request:
    types: [ opened, synchronize ]
    branches:
      - main
    paths-ignore:
      - "api/**"
      - "docs/**"
      - "deployments/**"
      - "features/**"
      - "scripts/**"
      - "*.md"

  workflow_dispatch:

jobs:
  build-hub:
    name: "Build and Push oecs-hub"
    uses: ./.github/workflows/build.yaml
    permissions:
      contents: read
      pull-requests: write
    with:
      service: oecs-hub
      push: ${{ github.event_name != 'pull_request' }}
      repository: oecs-hub
      dockerfile: "./build/service/Dockerfile"
    secrets: inherit