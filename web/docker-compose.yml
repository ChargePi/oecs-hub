services:
  web:
    build:
      context: .
    ports:
      - '8080:8080'
    environment:
      - BACKEND_URL=${BACKEND_URL:-http://localhost:9999}
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:8080/healthz']
      interval: 30s
      timeout: 3s
      retries: 3
